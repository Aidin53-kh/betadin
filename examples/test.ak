let x = fn() {
    return module {
        const name = "aidin";
    };
};

x = x();

let y = if x::name.len() > 5 {
    return "if block";
} else {
    return "else block";
};

println(y);

let t = @(1, 2, 3);

for i in t {
    
}

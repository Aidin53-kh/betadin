use std::math;
use std::collections::set;
use std::string::len;

let power = pow(4, 3);
println(add("4 power 3 = ", power));

let list = [2, 2, 5, 6, 5, 4, 6];
println(list);

let list_set = set(list);
println(list_set);

